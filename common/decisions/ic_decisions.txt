ic_territory_decisions = {
    possible = {
		is_subject = no
		
		aggressive_diplomatic_plays_permitted = yes
		
		any_country = {
			NOT = { THIS = ROOT }
			is_country_type = recognized
			aggressive_diplomatic_plays_permitted = yes
			is_subject = no
			save_scope_as = scope:opposite_nation
			any_neighbouring_state = {
				count >= 1
				has_claim_by = root
			}
		}
    }
    when_taken = {
        trigger_event = {id = ic_territory.1 popup = yes}
		set_variable = recent_ic
    }
	ai_chance = {value = 90}
}