test_decision = {
	is_shown = {
		always = yes
	}

	possible = {
			is_subject = no
			
			aggressive_diplomatic_plays_permitted = yes
			any_country = {
						#is_country_type = recognized
						
					
						NOR = {
							is_subject = yes
							
							is_country_type = decentralized
							THIS = ROOT
						}
						any_neighbouring_state = {
							
						
							has_claim_by = root
						}
						save_temporary_scope_as = crisis_temp
			}
			any_neighbouring_state = {
				count >= 1
				owner = scope:crisis_temp
				has_claim_by = root
			}
			NOR = {

				is_country_type = decentralized
			}


	}

	when_taken = {
		every_country = {
			limit = {
				NOT = {
					is_subject = yes
			
					is_country_type = decentralized
					THIS = ROOT
				}
				any_neighbouring_state = {
					has_claim_by = root
					
				}
			}
			save_scope_as = crisis_opposite
			trigger_event = ic_territory.1
		}
		this = {
			trigger_event = ic_territory.1
			save_scope_as = crisis_target
		}

	}

	ai_chance = {
		value = 0
	}
}

test_decision.2 = {
	is_shown = {
		always = yes
	}

	possible = {

			is_subject = no
			
			aggressive_diplomatic_plays_permitted = yes
			any_neighbouring_state = {
				has_claim_by = root

				count >= 1
				any_country = {
					count >= 1
					#is_country_type = recognized
					aggressive_diplomatic_plays_permitted = yes
					is_subject = no
				
					NOR = {
						has_variable = crisis_cooldown
						is_country_type = decentralized
						THIS = ROOT
					}
				}
			}
			NOR = {
				has_variable = crisis_cooldown
				is_country_type = decentralized
			}

	}

	when_taken = {
			every_neighbouring_state = {
				limit = {
					has_claim_by = root


					any_country = {

						#is_country_type = recognized
						aggressive_diplomatic_plays_permitted = yes
						is_subject = no
					
						NOR = {
							has_variable = crisis_cooldown
							is_country_type = decentralized
							THIS = ROOT
						}
						set_variable = {name = ic_opposite_nation}
					}
				}
				
			}
	}

	ai_chance = {
		value = 10
	}
}
