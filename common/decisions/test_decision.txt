test_decision = {
	is_shown = {
		always = yes
	}

	possible = {
			is_subject = no
			
			aggressive_diplomatic_plays_permitted = yes
			any_country = {
						#is_country_type = recognized
						
					
						NOR = {
							is_subject = yes
							
							is_country_type = decentralized
							THIS = ROOT
						}
						any_neighbouring_state = {
							
						
							has_claim_by = root
						}
						save_temporary_scope_as = crisis_temp
			}
			any_neighbouring_state = {
				count >= 1
				owner = scope:crisis_temp
				has_claim_by = root
			}
			NOR = {

				is_country_type = decentralized
			}


	}

	when_taken = {
		every_country = {
			limit = {
				NOT = {
					is_subject = yes
			
					is_country_type = decentralized
					THIS = ROOT
				}
				any_neighbouring_state = {
					has_claim_by = root
					owner = root
					
				}
			}
			save_scope_as = crisis_opposite
			trigger_event = ic_territory.2
			set_global_variable = {
				name = crisis_opposite
				value = scope:crisis_opposite
			}
		}
		this = {
		
			save_scope_as = crisis_target
			set_global_variable = {
				name = crisis_target
				value = scope:crisis_target
			}
		}
		every_neighbouring_state = {
			limit = {
				has_claim_by = scope:crisis_target
				owner = scope:crisis_opposite
			}
			set_global_variable = {
				name = terri_crisis_land
				value = this
			}
			save_scope_as = terri_crisis_land
		}

	}

	ai_chance = {
		value = 0
	}
}


